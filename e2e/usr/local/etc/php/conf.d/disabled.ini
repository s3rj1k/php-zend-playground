; Disabled WAF extension loaded but inactive. Baseline cost of having the
; extension in the process (hooks installed, but RINIT/RSHUTDOWN early return
; because waf.enabled=0).
[waf]
waf.enabled = 0

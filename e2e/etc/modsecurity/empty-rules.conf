# Empty rules file for the "enabled, no rules" scenario. The extension still
# initializes ModSecurity and runs the per request hooks/transaction lifecycle,
# but with zero rules loaded so no matching occurs.
# (intentionally empty)
